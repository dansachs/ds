export declare function appModule({ logPath, nextServerUrl, nextStaticRoot, }: {
    logPath: string;
    nextServerUrl?: string;
    nextStaticRoot?: string;
}): import("express-serve-static-core").Express;
