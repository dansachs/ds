import { DEngineClient, DVault, NoteProps, PodConfig, WorkspaceOpts } from "@dendronhq/common-all";
import { DLogger } from "@dendronhq/common-server";
import { URI } from "vscode-uri";
import { PodKind } from "./types";
export declare type PodOpts<T> = {
    engine: DEngineClient;
    config: T;
} & WorkspaceOpts;
export declare type PublishPodExecuteOpts<T extends PublishPodConfig = any> = PodOpts<T>;
export declare type PublishPodPlantOpts<T extends PublishPodConfig = any> = PublishPodExecuteOpts<T> & {
    note: NoteProps;
};
export declare type PublishPodConfig = {
    /**
     *  Name of file to publish
     */
    fname: string;
    /**
     * Name of vault
     */
    vaultName: string;
    /**
     * Where to write output to
     */
    dest: string | "stdout";
};
export declare abstract class PublishPod<T extends PublishPodConfig = any> {
    static kind: PodKind;
    get config(): PodConfig[];
    execute(opts: PublishPodExecuteOpts<T>): Promise<string>;
    abstract plant(opts: PublishPodPlantOpts<T>): Promise<string>;
}
export declare type ImportPodConfig = {
    /**
     * Where to import from
     */
    src: string;
    /**
     * Name of vault
     */
    vaultName: string;
    concatenate?: boolean;
    destName?: string;
    ignore?: boolean;
    frontmatter?: any;
    fnameAsId?: boolean;
};
export declare type ImportPodExecuteOpts<T extends ImportPodConfig = ImportPodConfig> = PodOpts<T>;
export declare type ImportPodPlantOpts<T extends ImportPodConfig = ImportPodConfig> = Omit<ImportPodExecuteOpts<T>, "src"> & {
    src: URI;
    vault: DVault;
};
export declare abstract class ImportPod<T extends ImportPodConfig = ImportPodConfig> {
    L: DLogger;
    static kind: PodKind;
    get config(): PodConfig[];
    constructor();
    validate(config: Partial<T>): void;
    execute(opts: ImportPodExecuteOpts<T>): Promise<NoteProps[]>;
    abstract plant(opts: ImportPodPlantOpts<T>): Promise<NoteProps[]>;
}
export declare type ExportPodConfig = {
    /**
     * Where to export to
     */
    dest: string;
    includeBody?: boolean;
    includeStubs?: boolean;
    ignore?: string[];
};
export declare type ExportPodExecuteOpts<T extends ExportPodConfig = ExportPodConfig> = PodOpts<T>;
export declare type ExportPodPlantOpts<T extends ExportPodConfig = ExportPodConfig> = Omit<ExportPodExecuteOpts<T>, "dest"> & {
    dest: URI;
    vaults: DVault[];
    notes: NoteProps[];
    wsRoot: string;
};
export declare abstract class ExportPod<T extends ExportPodConfig = ExportPodConfig, TData = any> {
    L: DLogger;
    static kind: PodKind;
    get config(): PodConfig[];
    constructor();
    validate(config: Partial<T>): void;
    /**
     * Checks for some pre-sets
     * - if not `includeBody`, then fetch notes without body
     * - if not `includeStubs`, then ignore stub nodes
     */
    prepareNotesForExport({ config, notes, }: {
        config: ExportPodConfig;
        notes: NoteProps[];
    }): NoteProps[];
    execute(opts: ExportPodExecuteOpts<T>): Promise<{
        notes: NoteProps[];
        data?: TData | undefined;
    }>;
    abstract plant(opts: ExportPodPlantOpts<T>): Promise<{
        notes: NoteProps[];
        data?: TData;
    }>;
}
