import { DVault, NoteProps } from "@dendronhq/common-all";
import { RefactorBaseCommand, RefactorRule } from "./refactorBase";
declare type CommonOpts = {
    overwriteFields?: string[];
};
declare type TFile = NoteProps;
export declare class RefactorFMCommand extends RefactorBaseCommand<TFile, any> {
    constructor();
    matchFile(_file: TFile): {
        isMatch: boolean;
    };
    refactorFile(file: TFile, rule: RefactorRule): TFile;
    readFile(fpath: string): NoteProps;
    writeFile(_fpath: string, _data: TFile): void;
}
export declare type RefactorFMCliOpts = {
    vault: DVault;
} & CommonOpts;
export {};
