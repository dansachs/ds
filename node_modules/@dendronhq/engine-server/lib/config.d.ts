import { CleanDendronSiteConfig, DendronConfig, DendronSiteConfig } from "@dendronhq/common-all";
export declare class ConfigUtils {
    static usePrettyRef(config: DendronConfig): boolean;
}
export declare class DConfig {
    static configPath(configRoot: string): string;
    static defaults(config: DendronConfig): DendronConfig;
    static genDefaultConfig(): DendronConfig;
    /**
     * Get without filling in defaults
     * @param wsRoot
     */
    static getRaw(wsRoot: string): Partial<DendronConfig>;
    static getOrCreate(dendronRoot: string, defaults?: Partial<DendronConfig>): DendronConfig;
    /**
     * Get config value with consideration for defaults
     * @param config
     */
    static getProp<K extends keyof DendronConfig>(config: DendronConfig, key: K): DendronConfig[K];
    static getSiteIndex(sconfig: DendronSiteConfig): string;
    /**
     * fill in defaults
     */
    static cleanSiteConfig(config: DendronSiteConfig): CleanDendronSiteConfig;
    static writeConfig({ wsRoot, config, }: {
        wsRoot: string;
        config: DendronConfig;
    }): void;
}
